package com.vb.controleveiculosclientes.api.controller;

import javax.validation.Valid;

import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.ResponseStatus;

import com.vb.controleveiculosclientes.domain.model.Cliente;
import com.vb.controleveiculosclientes.domain.model.Controle;

public class ControleController {

	
	/*@PostMapping
	@ResponseStatus(HttpStatus.CREATED)
	public Controle cadastro (@Valid @RequestBody Controle controle) {
		return cadastroControle.salvar(controle);
	}
	*/
}
